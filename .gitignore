# Rust build artifacts
target/
/target/

# Release distribution packages (generados por release.sh)
dist/

# Secrets and credentials — NUNCA commitear
*.env
.env
.env*
*.key
*.pem
*.p12
*.pfx
session.json
providers.env
providers.toml

# Enola user data
/temp_services/
*.log
/tmp/

# Generated session context (docs/dev/session_start.sh)
/_session_context.md
/tmp/enola_session_context.md
/tmp/enola_user_session_context.md

# OS files
.DS_Store
Thumbs.db
desktop.ini
*~

# IDE
.idea/
.vscode/
*.swp
*.swo

# Notas personales del desarrollador
NOTES.md

/PROJECT_CONTEXT.md
.ai_context/

# Python cache
__pycache__/
*.pyc

# ─── Archivos privados del propietario (NO commitear) ───────────────────────
# Contexto IA y herramientas de desarrollo privado del propietario.
# El repo es público pero estas herramientas son solo para desarrollo local.

# Archivos de contexto IA (raíz + anidados por directorio)
**/AGENTS.md
.github/copilot-instructions.md

# Project Brain (privado — roles, skills, memoria, workflows)
.agents/

# Herramientas de desarrollo (audit, build, test, session_start, release)
docs/dev/
scripts/dev/

# Plantillas de desarrollo privado (.github)
.github/ISSUE_TEMPLATE/*
!.github/ISSUE_TEMPLATE/bug_report.yml
!.github/ISSUE_TEMPLATE/config.yml
.github/PULL_REQUEST_TEMPLATE.md

# Minisign private key
feed/minisign.key

# Security audit reports (private, not for public repo)
security-audit/

# Forgejo workflows (disabled, private)
.forgejo/

# Cargo build config (contains developer-specific paths)
.cargo/config.toml
tests/
